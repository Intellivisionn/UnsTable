import 'package:flutter/material.dart';

class CustomColor {
  static const Color scaffoldBg = Color(0xFF121212); // Dark gray background
  static const Color primaryColor =
      Color(0xFF0070C0); // LINAK blue for primary actions
  static const Color accentColor =
      Color(0xFF0070C0); // LINAK blue for secondary actions
  static const Color errorColor = Color(0xFFCF6679); // Red for errors
  static const Color textColor =
      Color(0xFFE0E0E0); // Light gray for primary text
  static const Color hintColor =
      Color(0xFFB0BEC5); // Gray for hints or secondary text
  static const Color borderColor = Color(0xFF333333); // Dark border color
  static const Color cardColor =
      Color(0xFF1E1E1E); // Slightly lighter gray for cards

  // For buttons, links, etc.
  static const Color buttonColor = Color(0xFF0070C0); // LINAK blue for buttons
  static const Color linkColor = Color(0xFF0070C0); // LINAK blue for links
}
